export type RepoContents = {
    name: string;
    path: string;
    download_url: string;
}[];

export type LayoutContent = {
    slug: string;
    name: string;
    url: string;
    description: string;
    thumbnail: string;
    tag: string;
};
